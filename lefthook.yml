pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: bun run lint
    typecheck:
      run: bun run typecheck

pre-push:
  commands:
    test:
      run: bun run test
